{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CodeBlockScene",
  "description": "Syntax-highlighted code with animated reveal, line highlighting, and focus mode. Powered by remotion-bits CodeBlock.",
  "type": "object",
  "required": ["code", "beats"],
  "properties": {
    "code": {
      "type": "string",
      "description": "Source code to display. Multi-line strings with \\n line breaks."
    },
    "language": {
      "type": "string",
      "default": "javascript",
      "description": "Programming language passed through to the remotion-bits highlighter. Any string is accepted; the exact set of highlightable languages depends on the remotion-bits/Prism bundle (not validated here). Common values: javascript, typescript, jsx, tsx, python, bash, json, html, css, markdown, go, rust, cpp."
    },
    "theme": {
      "type": "string",
      "enum": ["dark", "light"],
      "default": "dark",
      "description": "Color theme for the code block."
    },
    "revealType": {
      "type": "string",
      "enum": ["lineByLine", "typing", "highlight", "fade"],
      "default": "lineByLine",
      "description": "How the code appears. lineByLine: lines reveal sequentially. typing: typewriter effect (no syntax highlighting, no line numbers, ignores highlightLines/focusLines). highlight: whole block fades in with highlightLines shown as static backgrounds. fade: simple fade in."
    },
    "highlightLines": {
      "type": "array",
      "items": { "type": "integer", "minimum": 1 },
      "default": [],
      "description": "Line numbers to highlight (1-indexed). Highlighted lines get a subtle background color."
    },
    "focusLines": {
      "type": "array",
      "items": { "type": "integer", "minimum": 1 },
      "default": [],
      "description": "Line numbers to focus on (1-indexed). Non-focused lines are dimmed and blurred."
    },
    "typingSpeed": {
      "type": "number",
      "minimum": 0.1,
      "maximum": 5.0,
      "default": 1.0,
      "description": "Typing/reveal speed multiplier. Higher = faster."
    },
    "showLineNumbers": {
      "type": "boolean",
      "default": true,
      "description": "Whether to show line numbers."
    },
    "fontSize": {
      "type": "integer",
      "minimum": 10,
      "maximum": 48,
      "description": "Font size in pixels. Auto-calculated if omitted."
    },
    "title": {
      "type": "string",
      "description": "Optional title displayed above the code block."
    },
    "beats": {
      "type": "object",
      "required": ["start"],
      "properties": {
        "start": { "type": "number", "description": "When the code starts appearing (seconds)" },
        "exit": { "type": "number", "description": "When the code starts exiting (seconds)" },
        "hold": { "type": "number", "description": "How long to hold before exit (seconds)" }
      }
    },
    "position": {
      "type": "object",
      "description": "Slot position from layout resolver",
      "properties": {
        "left": { "type": "number" },
        "top": { "type": "number" },
        "width": { "type": "number" },
        "height": { "type": "number" }
      }
    },
    "style": {
      "type": "object",
      "description": "Optional style overrides",
      "properties": {
        "container": { "type": "object" },
        "title": { "type": "object" },
        "codeBlock": { "type": "object" },
        "code": { "type": "object" }
      }
    }
  },
  "examples": [
    {
      "code": "function fibonacci(n) {\n  if (n <= 1) return n;\n  return fibonacci(n - 1) + fibonacci(n - 2);\n}",
      "language": "javascript",
      "revealType": "lineByLine",
      "highlightLines": [3],
      "title": "Recursive Fibonacci",
      "beats": { "start": 0.5 }
    },
    {
      "code": "git add .\ngit commit -m \"initial commit\"\ngit push origin main",
      "language": "bash",
      "revealType": "typing",
      "theme": "dark",
      "typingSpeed": 1.5,
      "beats": { "start": 0.3 }
    }
  ],
  "useCase": "Teaching programming concepts, showing code examples, demonstrating CLI commands, explaining algorithms step-by-step with line highlighting."
}
