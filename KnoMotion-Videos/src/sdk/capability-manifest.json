{
  "version": "1.1",
  "engine": "KnoMotion",
  "remotionVersion": "4.0.382",

  "canonicalMidSceneKeys": [
    "textReveal", "heroText", "gridCards", "checklist", "bubbleCallout",
    "sideBySide", "iconGrid", "cardSequence", "bigNumber", "animatedCounter", "codeBlock"
  ],

  "knownIssues": [
    {
      "id": "midscene-aliases-not-resolved",
      "severity": "warning",
      "summary": "Only the 11 canonicalMidSceneKeys are resolved at render time by SceneRenderer.jsx (MID_SCENE_COMPONENTS). The per-mid-scene 'aliases' listed below — and the alias entries in the renderer Zod schema (sdk/schemas/videoConfig.schema.ts MidSceneKeys, e.g. 'textRevealSequence', 'gridCardReveal', 'bigNumberReveal', 'codeBlockScene', 'code') — pass schema validation but are NOT mapped to a component, so they render an empty slot.",
      "guidance": "Generators MUST emit only canonical keys. Either tighten the renderer Zod MidSceneKeys to the 11 canonical keys, or add alias normalisation in SceneRenderer. Until then, treat aliases as unsupported."
    }
  ],

  "midScenes": {
    "textReveal": {
      "supported": true,
      "component": "TextRevealSequence",
      "aliases": ["textRevealSequence"],
      "requiredFields": ["lines", "beats"],
      "keyConfig": {
        "lines": "Array of { text, emphasis? } — minItems: 1",
        "revealType": "typewriter | fade | slide | mask | blurSlide | charByChar | glitchIn | variableTypewriter | glitchCycle (default: fade). blurSlide/charByChar/glitchIn/variableTypewriter/glitchCycle are remotion-bits powered.",
        "direction": "For slide: up | down | left | right (default: up). For mask: left | right | top | bottom | center (up/down produce no clip-path).",
        "showCursor": "Boolean — blinking cursor during typewriter reveal (default: true)",
        "staggerDelay": "Seconds between lines (default: 0.2)",
        "lineSpacing": "tight | normal | relaxed | loose (default: normal)",
        "linesBeats": "Per-line beats { start, hold, exit, emphasis } preferred over container beats"
      }
    },
    "heroText": {
      "supported": true,
      "component": "HeroTextEntranceExit",
      "aliases": ["heroTextEntranceExit"],
      "requiredFields": ["heroType", "heroRef", "beats"],
      "keyConfig": {
        "heroType": "image | lottie | svg | roughSVG | emoji | custom (default: image)",
        "heroRef": "URL, asset path, lottie registry key, or emoji depending on heroType",
        "text": "Optional hero title text",
        "beats": "Requires exit and one of entrance/start (start is an alias for entrance)"
      }
    },
    "gridCards": {
      "supported": true,
      "component": "GridCardReveal",
      "aliases": ["gridCardReveal", "cardGrid"],
      "requiredFields": ["cards", "beats"],
      "keyConfig": {
        "cards": "Array of card objects { icon?, image?, label?, sublabel?, color?, variant?, layout?, animated?, beats? } — minItems: 1",
        "columns": "1–8 (default: auto from card count)",
        "animation": "fade | slide | scale | bounce | flip | mask | cascade | centerRipple (+ fadeIn/slideIn/scaleIn/bounceIn aliases) (default: cascade)",
        "cardVariant": "default | bordered | glass | elevated | gradient | flat (flat renders as default) (default: default)"
      }
    },
    "checklist": {
      "supported": true,
      "component": "ChecklistReveal",
      "aliases": ["checklistReveal"],
      "requiredFields": ["items", "beats"],
      "keyConfig": {
        "items": "Array of { text, checked?, icon?, color?, animated?, beats? } or plain strings — minItems: 1",
        "revealType": "pop | slide | fade | scale | spring | bounceIn | slideIn | scaleIn | fadeIn | listReveal (default: pop). listReveal is remotion-bits powered.",
        "icon": "Static presets: check | checkmark | tick | bullet | dot | arrow | star | diamond | circle | square | plus | minus. Lottie presets: lottieCheck | lottieTick | lottieSuccess | lottieStar. Or a custom emoji string. (default: check)",
        "iconColor": "Theme key (primary, secondary, accentGreen, accentBlue, doodle) or hex/rgb (default: accentGreen)"
      }
    },
    "bubbleCallout": {
      "supported": true,
      "component": "BubbleCalloutSequence",
      "aliases": ["bubbleCalloutSequence", "callouts"],
      "requiredFields": ["callouts", "beats"],
      "keyConfig": {
        "callouts": "Array of { text, icon?, color?, shape?, animated?, beats? } or plain strings — minItems: 1",
        "shape": "speech | rounded | notebook | pill (default: speech)",
        "pattern": "scattered | zigzag | diagonal (default: scattered)",
        "animation": "pop | float | slide | scale | fade (default: float)",
        "collisionDetection": "Boolean — avoids overlapping callouts (default: true)",
        "jitter": "{ x, y } pixel offsets for organic placement"
      }
    },
    "sideBySide": {
      "supported": true,
      "component": "SideBySideCompare",
      "aliases": ["sideBySideCompare", "compare"],
      "requiredFields": ["beats"],
      "keyConfig": {
        "left": "{ title?, subtitle?, icon?, items?, color?, backgroundColor? } — left panel content",
        "right": "{ title?, subtitle?, icon?, items?, color?, backgroundColor? } — right panel content",
        "mode": "standard | beforeAfter (default: standard). 'beforeAfter' uses before/after media + slider.",
        "animation": "slide | fade | scale | bounce | reveal (default: slide)",
        "dividerType": "none | line | dashed | vs (default: vs)",
        "alignment": "center | inner (default: center)"
      }
    },
    "iconGrid": {
      "supported": true,
      "component": "IconGrid",
      "aliases": [],
      "requiredFields": ["icons", "beats"],
      "keyConfig": {
        "icons": "Array of { iconRef, label?, color?, animated?, beats? } — minItems: 1",
        "columns": "1–8 (default: 4)",
        "iconSize": "sm | md | lg | xl (default: lg)",
        "animation": "fadeIn | slideIn | scaleIn | bounceIn | cascade (default: scaleIn)"
      }
    },
    "cardSequence": {
      "supported": true,
      "component": "CardSequence",
      "aliases": [],
      "requiredFields": ["cards", "beats"],
      "keyConfig": {
        "cards": "Array of { title?, content?, variant?, size?, beats? } — minItems: 1",
        "layout": "stacked | grid (default: stacked)",
        "columns": "1–6 (for grid layout) (default: 3)",
        "animation": "fadeIn | slideIn | scaleIn | fadeSlide (default: fadeSlide)",
        "variant": "default | bordered | glass",
        "size": "sm | md | lg (default: md)"
      }
    },
    "bigNumber": {
      "supported": true,
      "component": "BigNumberReveal",
      "aliases": ["bigNumberReveal"],
      "requiredFields": ["number", "beats"],
      "keyConfig": {
        "number": "String — the display number (pre-formatted)",
        "label": "Description text below the number",
        "emphasis": "high | normal | low (default: high) — selects number color tier",
        "animation": "pop | countUp | typewriter | fade (default: pop)",
        "countFrom": "Number — start value for countUp animation",
        "color": "Literal CSS color only (hex/rgb/hsl). Does NOT resolve theme keys."
      }
    },
    "animatedCounter": {
      "supported": true,
      "component": "AnimatedCounter",
      "aliases": [],
      "requiredFields": ["startValue", "endValue", "beats"],
      "keyConfig": {
        "startValue": "Number — count start",
        "endValue": "Number — count end",
        "duration": "Seconds for the count animation",
        "prefix": "String — e.g. '$'",
        "suffix": "String — e.g. '%'",
        "color": "Literal CSS color only (hex/rgb/hsl). Does NOT resolve theme keys."
      }
    },
    "codeBlock": {
      "supported": true,
      "component": "CodeBlockScene",
      "aliases": ["codeBlockScene", "code"],
      "requiredFields": ["code", "beats"],
      "keyConfig": {
        "code": "String — source code with \\n line breaks",
        "language": "Any string, passed through to the remotion-bits highlighter (default: javascript). Highlightable set depends on the bundle; not validated. Common: javascript, typescript, jsx, tsx, python, bash, json, html, css, markdown, go, rust, cpp.",
        "revealType": "lineByLine | typing | highlight | fade (default: lineByLine)",
        "theme": "dark | light (default: dark)",
        "highlightLines": "Array of line numbers to highlight (1-indexed)",
        "focusLines": "Array of line numbers to focus (dims others)",
        "typingSpeed": "Speed multiplier 0.1–5.0 (default: 1.0)",
        "showLineNumbers": "Boolean (default: true)",
        "title": "Optional title above the code block"
      }
    }
  },

  "layouts": [
    {
      "type": "full",
      "description": "Single content area with optional header",
      "slots": ["header", "full"]
    },
    {
      "type": "rowStack",
      "description": "Vertical sections stacked",
      "slots": ["header", "row1", "row2", "..."],
      "options": { "rows": "Number of rows (default: 2)" }
    },
    {
      "type": "columnSplit",
      "description": "Side-by-side columns (auto-converts to rowStack on mobile)",
      "slots": ["header", "col1", "col2", "..."],
      "options": { "columns": "Number of columns (default: 2)" }
    },
    {
      "type": "headerRowColumns",
      "description": "Complex layout: header + row + columns below",
      "slots": ["header", "row", "col1", "col2", "..."]
    },
    {
      "type": "gridSlots",
      "description": "Grid layout with named cells",
      "slots": ["header", "cellA", "cellB", "..."]
    }
  ],

  "backgrounds": [
    { "preset": "notebookSoft", "description": "Lined paper overlay" },
    { "preset": "sunriseGradient", "description": "Warm diagonal gradient" },
    { "preset": "cleanCard", "description": "Neutral white" },
    { "preset": "chalkboardGradient", "description": "Dark blue-green gradient" },
    { "preset": "spotlight", "description": "Vignette with configurable focus point" },
    { "preset": "custom", "description": "Direct CSS style object" }
  ],

  "backgroundOptions": {
    "layerNoise": "Boolean — adds subtle film grain overlay",
    "particles": {
      "enabled": "Boolean",
      "style": "dots | chalk | snow | sparkle",
      "count": "Number (default: 20)",
      "color": "CSS color or theme key",
      "opacity": "0–1"
    }
  },

  "transitions": [
    { "type": "fade", "status": "active", "description": "Opacity crossfade" },
    { "type": "slide", "status": "active", "description": "Directional push with spring physics", "directions": ["up", "down", "left", "right"] },
    { "type": "page-turn", "status": "active", "description": "3D flip with perspective", "directions": ["left", "right"] },
    { "type": "clock-wipe", "status": "active", "description": "Circular reveal like a clock hand" },
    { "type": "iris", "status": "active", "description": "Circular mask expanding from center" },
    { "type": "doodle-wipe", "status": "legacy-fallback", "description": "Falls back to slide()" },
    { "type": "eraser", "status": "legacy-fallback", "description": "Falls back to slide()" },
    { "type": "spring", "status": "legacy-fallback", "description": "Falls back to slide()" }
  ],

  "stylePresets": ["educational", "playful", "minimal", "mentor", "focus"],

  "emphasisLevels": ["high", "normal", "low"],

  "captionStyles": ["tiktok", "subtitle", "karaoke"],

  "audioChannels": {
    "narration": "TTS audio file with optional startFromSeconds offset",
    "music": "Background music with fade-in/out, loops by default",
    "sfx": "Array of sound effects, each at a specific atSecond offset"
  },

  "lottieKeys": [
    "success", "checkmark", "loading", "error",
    "confetti", "celebration", "fireworks", "sparkles", "stars",
    "lightbulb", "thinking", "question", "brain", "book",
    "waving", "walking", "thumbs-up", "clapping",
    "arrow-right", "arrow-down", "swipe",
    "brain-active", "funnel-filter", "lightning-bolt", "target-focus",
    "clock-delay", "signal-buffer",
    "atom", "dna", "planet", "rocket",
    "music-notes", "equalizer", "headphones",
    "laptop", "phone", "wifi",
    "sunny", "cloudy", "rainy", "snowy"
  ],

  "constraints": {
    "fpsFixed": 30,
    "beatsInSeconds": true,
    "transitionFramesDefault": 20,
    "maxScenesPerVideo": 20,
    "maxCardsInGrid": 8,
    "maxColumnsInGrid": 8,
    "maxChecklistItems": 12,
    "maxTextLines": 8,
    "maxCallouts": 10,
    "desktopDimensions": { "width": 1920, "height": 1080 },
    "mobileDimensions": { "width": 1080, "height": 1920 }
  },

  "unsupported": [
    "Embedded video playback (no <Video> inside scenes)",
    "Interactive/branching elements (no user input)",
    "Real-time data binding (all content must be static at render time)",
    "3D scenes (no WebGL/Three.js integration)",
    "Custom font loading from arbitrary URLs",
    "Variable frame rates (fixed at 30fps)",
    "Audio recording/microphone input",
    "Screen recording/capture",
    "Multi-track video overlays (only single visual layer per scene)"
  ]
}
